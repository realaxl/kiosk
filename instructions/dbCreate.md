create a folder "db" in the project directory 
check if a file with a sqlite db named "db.sqlite" exists
if it exists, run section Amend DB below

# Create new DB file
check the ERM in assets folder and create the tables in a new sqlite db file db.sqlite
create all tables according to the ERM
also create primary and foreign keys
and stop here

# Amend DB
create missing tables according to the ERM
check all the tables for missing columns, create them, if neccessary
also create primary and foreign keys