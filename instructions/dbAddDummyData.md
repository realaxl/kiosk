check if a file with a sqlite db named "db.sqlite" exists
if it does not exist, stop here


check the filenames in images folder
use the filenames to create the table entries in the database as products.
add the filename of the image to the imageUrl column in the products table
use the product part of the filename as the name column in the products table
use "auto-gnerated dummy" as note and description
set the entries active with the value 1
